# Python bytecode (the markdown hooks are plain scripts; bytecode is build cruft)
__pycache__/
*.pyc

# Hook runtime state
.claude/hooks/markdown/state/

# Derived pack enablement (materialized from harness.config.json packs[])
.claude/enabled-packs.json

# Copier answers file exists only in an instantiated harness, not the template repo
.copier-answers.yml
