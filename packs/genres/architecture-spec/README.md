# architecture-spec (genre pack)

An AI-ready architecture-specification genre (arc42/C4): cross-cutting entity types,
relationships, namespaces, decisions-with-alternatives, and EARS acceptance criteria a
downstream AI coding agent can execute. Rendered by the `ai-spec` channel. Optional, toggle-ready
(`enabled:false`). See `skills/architecture-spec/SKILL.md`.
